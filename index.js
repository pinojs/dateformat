'use strict'

const dateFormat = require('./lib/date-format.js')
const dateFormatter = require('./lib/date-formatter.js')

module.exports = {
    dateFormat,
    dateFormatter
}

module.exports.default = {
    dateFormat,
    dateFormatter
}